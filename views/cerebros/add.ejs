<% include ../_header %>
<div class="row container">
    <div class="col-md-12">
        <form action="new" method="POST">
            <div class="form-group">
                <label for=""> Sabor </label>
                <input type="text" class="form-control" name="flavor">
            </div>
            <div class="form-group">
                <label for="">Descripción</label>
                <input type="text" class="form-control" name="description">
            </div>
            <div class="form-group">
                <label for="">IQ</label>
                <input type="text" class="form-control" name="iq">
            </div>
            <div class="form-group">
                <label for="">Imagen</label>
                <input type="text" class="form-control" name="picture">
            </div>

            <% if(mensajeErrorC.length > 0){ %>

            <div class="alert alert-danger" role="alert">
                <%= mensajeErrorC %>
            </div>

            <% } %>

            <% if(mensajeExitoC.length > 0){ %>

                <div class="alert alert-success" role="alert">
                    <%= mensajeExitoC %>
                </div>
    
            <% } %>

            <button class="btn btn-primary"><i class="fas fa-brain"></i> Agregar Cerebro </button>
        </form>
    </div>
</div>
<% include ../_footer %>